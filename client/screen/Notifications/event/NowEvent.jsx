import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export default function NowEvent({navigation}) {
    
      return (
        <SafeAreaView style={{ flex: 1 }}>
          <Text>now event</Text>
        </SafeAreaView>
      );
}