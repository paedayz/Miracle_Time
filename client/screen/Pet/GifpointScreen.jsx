import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export default function GifpointScreen({navigation}) {
    
      return (
        <SafeAreaView style={{ flex: 1 }}>
          <Text>Gif pointScreen</Text>
        </SafeAreaView>
      );
}